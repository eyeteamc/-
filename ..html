<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>چشم‌انداز گویی | Eye Landgoi - ربات مترجم</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700;900&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --jade-dark: #0a3522;
            --jade-deep: #13532e;
            --jade-medium: #1d6b4a;
            --jade-light: #3a9b70;
            --jade-soft: #7fc9a2;
            --jade-pale: #d4f0e0;
            --gold: #c9b16b;
            --gold-light: #ead7a5;
            --cream: #f5efe0;
            --dark-bg: #071b10;
        }

        body {
            font-family: 'Vazirmatn', sans-serif;
            background: var(--dark-bg);
            color: #e6f3e6;
            overflow-x: hidden;
            position: relative;
        }

        /* ========== صفحه لودینگ ========== */
        #loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, #0a2f1a, #031008);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: opacity 1s ease, visibility 1s ease;
            opacity: 1;
            visibility: visible;
        }

        #loading-screen.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loading-content {
            text-align: center;
            position: relative;
        }

        .eye-loader {
            position: relative;
            width: 200px;
            height: 200px;
            margin: 0 auto 40px;
            perspective: 1000px;
        }

        .loader-orb {
            width: 100%;
            height: 100%;
            position: relative;
            transform-style: preserve-3d;
            animation: rotateOrb 8s infinite linear;
        }

        .loader-ring {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: 4px solid transparent;
            border-radius: 50%;
            border-top-color: var(--gold);
            border-bottom-color: var(--jade-light);
            animation: spinRing 2s infinite ease-in-out;
        }

        .loader-ring:nth-child(2) {
            width: 80%;
            height: 80%;
            top: 10%;
            left: 10%;
            border-top-color: var(--jade-soft);
            border-bottom-color: var(--gold-light);
            border-left-color: transparent;
            border-right-color: transparent;
            animation: spinRing 1.5s infinite reverse;
        }

        .loader-ring:nth-child(3) {
            width: 60%;
            height: 60%;
            top: 20%;
            left: 20%;
            border: 4px solid transparent;
            border-left-color: var(--cream);
            border-right-color: var(--jade-medium);
            animation: spinRing 1s infinite;
        }

        .loader-pupil {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 40px;
            height: 40px;
            background: radial-gradient(circle at 30% 30%, #fff, var(--gold));
            border-radius: 50%;
            box-shadow: 0 0 40px var(--gold);
            animation: pulsePupil 1.5s infinite alternate;
        }

        @keyframes rotateOrb {
            0% { transform: rotateY(0deg) rotateX(10deg); }
            100% { transform: rotateY(360deg) rotateX(10deg); }
        }

        @keyframes spinRing {
            0% { transform: rotate(0deg) scale(1); opacity: 0.8; }
            50% { transform: rotate(180deg) scale(1.1); opacity: 1; }
            100% { transform: rotate(360deg) scale(1); opacity: 0.8; }
        }

        @keyframes pulsePupil {
            0% { transform: translate(-50%, -50%) scale(1); box-shadow: 0 0 40px var(--gold); }
            100% { transform: translate(-50%, -50%) scale(1.2); box-shadow: 0 0 80px var(--gold-light); }
        }

        .loading-text {
            font-size: 2rem;
            font-weight: 900;
            background: linear-gradient(135deg, var(--gold), var(--jade-soft), white);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 20px;
            letter-spacing: 4px;
            text-shadow: 0 0 30px rgba(0,255,100,0.3);
        }

        .loading-progress {
            width: 300px;
            height: 6px;
            background: rgba(255,255,255,0.1);
            border-radius: 10px;
            margin: 20px auto;
            overflow: hidden;
            position: relative;
        }

        .loading-bar {
            height: 100%;
            width: 0%;
            background: linear-gradient(90deg, var(--jade-light), var(--gold));
            border-radius: 10px;
            animation: loadProgress 2.5s ease-in-out forwards;
            box-shadow: 0 0 20px var(--gold);
        }

        @keyframes loadProgress {
            0% { width: 0%; }
            20% { width: 35%; }
            50% { width: 70%; }
            80% { width: 90%; }
            100% { width: 100%; }
        }

        .loading-tip {
            color: var(--jade-soft);
            font-size: 1rem;
            margin-top: 20px;
            opacity: 0.8;
            animation: fadeTip 2s infinite;
        }

        @keyframes fadeTip {
            0%, 100% { opacity: 0.5; }
            50% { opacity: 1; }
        }

        /* ========== ربات شناسایی ========== */
        .robot-recognition {
            position: fixed;
            bottom: 30px;
            left: 30px;
            z-index: 1000;
            cursor: pointer;
            transition: all 0.3s;
            filter: drop-shadow(0 0 20px rgba(0,255,100,0.3));
        }

        .robot-recognition:hover {
            transform: scale(1.1) rotate(5deg);
            filter: drop-shadow(0 0 40px var(--gold));
        }

        .robot-eye {
            position: relative;
            width: 80px;
            height: 80px;
            background: radial-gradient(circle at 30% 30%, #1f543b, #0a281a);
            border-radius: 50%;
            border: 3px solid var(--gold);
            box-shadow: 0 0 30px rgba(0,255,100,0.5);
            animation: robotGlow 3s infinite;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        @keyframes robotGlow {
            0%, 100% { box-shadow: 0 0 30px rgba(100,255,150,0.5); }
            50% { box-shadow: 0 0 60px var(--gold); }
        }

        .robot-pupil {
            width: 35px;
            height: 35px;
            background: radial-gradient(circle at 40% 40%, #fff, var(--jade-light));
            border-radius: 50%;
            animation: lookAround 4s infinite;
            position: relative;
        }

        .robot-pupil::after {
            content: '';
            position: absolute;
            top: 10px;
            left: 10px;
            width: 10px;
            height: 10px;
            background: white;
            border-radius: 50%;
            opacity: 0.8;
        }

        @keyframes lookAround {
            0% { transform: translate(0, 0); }
            25% { transform: translate(10px, -5px); }
            50% { transform: translate(-5px, 10px); }
            75% { transform: translate(-10px, -5px); }
            100% { transform: translate(0, 0); }
        }

        .robot-message {
            position: absolute;
            bottom: 90px;
            left: 0;
            background: rgba(10, 40, 20, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid var(--gold);
            border-radius: 30px 30px 30px 5px;
            padding: 15px 25px;
            min-width: 200px;
            white-space: nowrap;
            color: white;
            font-weight: 500;
            box-shadow: 0 10px 30px black;
            border-left: 5px solid var(--gold);
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.3s;
            pointer-events: none;
        }

        .robot-recognition:hover .robot-message {
            opacity: 1;
            transform: translateY(0);
        }

        .robot-message::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 20px;
            width: 20px;
            height: 20px;
            background: rgba(10, 40, 20, 0.9);
            border-left: 1px solid var(--gold);
            border-bottom: 1px solid var(--gold);
            transform: rotate(-45deg);
            backdrop-filter: blur(10px);
        }

        /* ========== کتابخانه سه‌بعدی پیشرفته ========== */
        .library-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            opacity: 0.2;
            pointer-events: none;
            perspective: 2000px;
        }

        .floating-books {
            position: absolute;
            width: 100%;
            height: 100%;
            transform-style: preserve-3d;
            animation: floatLibrary 20s infinite linear;
        }

        @keyframes floatLibrary {
            0% { transform: rotateY(0deg) rotateX(5deg); }
            50% { transform: rotateY(180deg) rotateX(5deg); }
            100% { transform: rotateY(360deg) rotateX(5deg); }
        }

        .book-3d {
            position: absolute;
            width: 30px;
            height: 60px;
            background: linear-gradient(145deg, var(--jade-medium), var(--jade-dark));
            border-left: 5px solid var(--gold);
            border-right: 2px solid var(--jade-light);
            border-radius: 3px;
            transform-origin: center center;
            box-shadow: 0 0 30px rgba(0,255,100,0.2);
            animation: bookFloat 6s infinite alternate;
        }

        @keyframes bookFloat {
            0% { transform: translateZ(0px) rotateY(0deg); }
            100% { transform: translateZ(50px) rotateY(180deg); }
        }

        .book-3d:nth-child(1) { top: 20%; left: 10%; animation-delay: 0s; }
        .book-3d:nth-child(2) { top: 60%; left: 20%; animation-delay: 1s; height: 80px; }
        .book-3d:nth-child(3) { top: 30%; left: 70%; animation-delay: 2s; width: 40px; }
        .book-3d:nth-child(4) { top: 70%; left: 80%; animation-delay: 3s; height: 100px; }
        .book-3d:nth-child(5) { top: 40%; left: 40%; animation-delay: 4s; background: linear-gradient(145deg, var(--gold), #a58d4d); }
        .book-3d:nth-child(6) { top: 80%; left: 50%; animation-delay: 5s; }
        .book-3d:nth-child(7) { top: 15%; left: 85%; animation-delay: 6s; height: 120px; }
        .book-3d:nth-child(8) { top: 50%; left: 5%; animation-delay: 7s; }

        /* ========== کانتینر اصلی ========== */
        .main-content {
            position: relative;
            z-index: 10;
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px 20px;
            min-height: 100vh;
            opacity: 0;
            transform: translateY(30px);
            transition: all 1s ease;
        }

        .main-content.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* ========== هدر پیشرفته ========== */
        .advanced-header {
            background: rgba(8, 35, 18, 0.7);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(100, 200, 120, 0.3);
            border-radius: 40px 40px 30px 30px;
            padding: 20px 35px;
            margin-bottom: 40px;
            box-shadow: 0 25px 50px -15px black, 
                        0 0 0 1px var(--jade-light) inset,
                        0 0 30px rgba(0,255,100,0.1);
            transform-style: preserve-3d;
            transform: translateZ(20px);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 20px;
            position: relative;
            overflow: hidden;
        }

        .advanced-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at center, transparent 30%, rgba(0,255,100,0.03) 70%);
            animation: headerGlow 10s infinite;
        }

        @keyframes headerGlow {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .header-stats {
            display: flex;
            gap: 20px;
        }

        .stat-item {
            background: rgba(0, 20, 8, 0.6);
            padding: 10px 20px;
            border-radius: 30px;
            border: 1px solid var(--jade-light);
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .stat-number {
            color: var(--gold);
            font-weight: 900;
            font-size: 1.2rem;
        }

        /* ========== کتاب دو صفحه‌ای با افکت 3D ========== */
        .book-3d-container {
            display: flex;
            gap: 30px;
            flex: 1;
            perspective: 3000px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .book-page-advanced {
            flex: 1;
            min-width: 300px;
            background: linear-gradient(145deg, #1a412a, #0f2f1d);
            border-radius: 30px 30px 30px 30px;
            padding: 30px;
            box-shadow: 
                -20px 20px 40px rgba(0,0,0,0.8),
                0 0 0 2px var(--jade-light) inset,
                0 0 0 4px rgba(255,215,0,0.1) inset;
            transform: rotateY(var(--rotate-y, 0deg)) rotateX(2deg) translateZ(0);
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border-left: 10px solid var(--gold);
            border-right: 5px solid #2a623d;
            position: relative;
            backdrop-filter: blur(5px);
        }

        .book-page-advanced:hover {
            --rotate-y: -3deg;
            transform: rotateY(-3deg) rotateX(1deg) translateZ(20px);
            box-shadow: -25px 25px 60px black, 0 0 0 3px var(--jade-light) inset;
        }

        .book-page-advanced:last-child:hover {
            --rotate-y: 3deg;
        }

        .page-decoration {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 2rem;
            opacity: 0.1;
            transform: rotate(15deg);
        }

        /* ========== باکس‌های ترجمه با افکت ========== */
        .translation-box-advanced {
            background: rgba(5, 25, 12, 0.6);
            backdrop-filter: blur(10px);
            border-radius: 25px;
            padding: 20px;
            border: 1px solid #3f7a5a;
            box-shadow: inset 0 0 50px rgba(0,0,0,0.9), 0 10px 30px rgba(0,0,0,0.5);
            margin-bottom: 20px;
            position: relative;
            overflow: hidden;
        }

        .translation-box-advanced::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 20% 30%, rgba(100,255,150,0.1), transparent 70%);
            pointer-events: none;
        }

        .translation-label-advanced {
            display: flex;
            justify-content: space-between;
            color: var(--jade-soft);
            margin-bottom: 10px;
            font-size: 0.9rem;
            letter-spacing: 1px;
            position: relative;
            z-index: 2;
        }

        textarea {
            width: 100%;
            min-height: 180px;
            background: rgba(0, 20, 5, 0.5);
            border: none;
            border-radius: 20px;
            padding: 20px;
            color: #f0fff0;
            font-family: 'Vazirmatn', sans-serif;
            font-size: 1.1rem;
            line-height: 1.8;
            resize: vertical;
            outline: none;
            border: 1px solid transparent;
            transition: all 0.3s;
            direction: auto;
            position: relative;
            z-index: 2;
        }

        textarea:focus {
            border-color: var(--gold);
            box-shadow: 0 0 40px rgba(200,180,50,0.3);
            background: rgba(0, 30, 8, 0.7);
        }

        /* ========== دکمه‌های نئومورفیک ========== */
        .button-panel {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
            margin: 30px 0;
            justify-content: center;
        }

        .btn-advanced {
            background: linear-gradient(145deg, #1a4d31, #0e351f);
            border: none;
            padding: 18px 35px;
            border-radius: 60px;
            color: white;
            font-family: 'Vazirmatn', sans-serif;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s;
            border-bottom: 5px solid #0a2b19;
            border-right: 2px solid #3f8a64;
            display: inline-flex;
            align-items: center;
            gap: 12px;
            flex: 1;
            justify-content: center;
            min-width: 200px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0,0,0,0.5);
        }

        .btn-advanced::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
            transition: left 0.5s;
        }

        .btn-advanced:hover::before {
            left: 100%;
        }

        .btn-advanced:hover {
            transform: translateY(-5px);
            border-bottom-width: 7px;
            box-shadow: 0 15px 35px rgba(0,150,50,0.5);
        }

        .btn-advanced:active {
            transform: translateY(3px);
            border-bottom-width: 3px;
        }

        .btn-primary-advanced {
            background: linear-gradient(145deg, #2a7a50, #195f38);
            border-bottom-color: #0f4029;
            border-right-color: #7fd7aa;
            box-shadow: 0 5px 30px rgba(0,200,100,0.4);
        }

        .btn-gold-advanced {
            background: linear-gradient(145deg, #9e8749, #786532);
            border-bottom-color: #5a4b25;
            border-right-color: #f0d98c;
        }

        /* ========== دیکشنری پیشرفته ========== */
        .dictionary-advanced {
            background: rgba(8, 35, 16, 0.8);
            backdrop-filter: blur(15px);
            border-radius: 50px;
            padding: 35px;
            margin-top: 40px;
            border: 1px solid var(--jade-light);
            border-bottom: 5px solid var(--gold);
            box-shadow: 0 30px 60px -20px black;
        }

        .search-advanced {
            display: flex;
            gap: 10px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .search-input-advanced {
            flex: 1;
            min-width: 250px;
            background: rgba(0, 25, 8, 0.7);
            border: 2px solid #3d8660;
            border-radius: 60px;
            padding: 18px 30px;
            color: white;
            font-family: 'Vazirmatn', sans-serif;
            font-size: 1.1rem;
            transition: all 0.3s;
        }

        .search-input-advanced:focus {
            border-color: var(--gold);
            box-shadow: 0 0 30px rgba(255,215,0,0.3);
        }

        .dictionary-grid-advanced {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 15px;
            max-height: 450px;
            overflow-y: auto;
            padding: 15px;
            background: rgba(0, 20, 5, 0.3);
            border-radius: 40px;
        }

        .word-card-advanced {
            background: linear-gradient(145deg, #1d4a31, #123d24);
            padding: 18px;
            border-radius: 25px;
            border-right: 8px solid var(--gold);
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }

        .word-card-advanced:hover {
            transform: scale(1.03) translateX(-5px);
            background: #276a41;
            border-right-width: 12px;
            box-shadow: 0 10px 30px rgba(0,255,100,0.3);
        }

        .word-fa-advanced {
            font-weight: 700;
            color: var(--cream);
            font-size: 1.1rem;
        }

        .word-en-advanced {
            color: var(--jade-soft);
            font-size: 1rem;
        }

        /* ========== فوتر پیشرفته ========== */
        .advanced-footer {
            text-align: center;
            margin-top: 60px;
            padding: 30px;
            background: rgba(0, 20, 5, 0.5);
            backdrop-filter: blur(10px);
            border-radius: 60px;
            border: 1px solid var(--jade-light);
        }

        .team-signature-advanced {
            color: var(--gold);
            font-weight: 900;
            font-size: 1.4rem;
            text-shadow: 0 0 20px gold;
            margin: 15px 0;
            letter-spacing: 3px;
        }

        /* ========== انیمیشن‌های اضافی ========== */
        @keyframes floatParticle {
            0% { transform: translateY(0) translateX(0); opacity: 0; }
            50% { opacity: 0.5; }
            100% { transform: translateY(-100px) translateX(100px); opacity: 0; }
        }

        .particle {
            position: fixed;
            width: 4px;
            height: 4px;
            background: var(--gold);
            border-radius: 50%;
            pointer-events: none;
            opacity: 0;
            animation: floatParticle 8s infinite;
        }
    </style>
</head>
<body>
    <!-- ذرات معلق -->
    <div id="particles-container"></div>

    <!-- صفحه لودینگ -->
    <div id="loading-screen">
        <div class="loading-content">
            <div class="eye-loader">
                <div class="loader-orb">
                    <div class="loader-ring"></div>
                    <div class="loader-ring"></div>
                    <div class="loader-ring"></div>
                    <div class="loader-pupil"></div>
                </div>
            </div>
            <h1 class="loading-text">چشم‌انداز گویی</h1>
            <div class="loading-progress">
                <div class="loading-bar"></div>
            </div>
            <p class="loading-tip">در حال بارگذاری کتابخانه هوشمند...</p>
            <p class="loading-tip" id="loading-tip-text">ربات شناسایی در حال فعال‌سازی</p>
        </div>
    </div>

    <!-- ربات شناسایی -->
    <div class="robot-recognition" id="robot-recognition">
        <div class="robot-eye">
            <div class="robot-pupil"></div>
        </div>
        <div class="robot-message" id="robot-message">
            👋 سلام! من ربات چشم‌انداز گویی هستم
        </div>
    </div>

    <!-- پس‌زمینه کتابخانه سه‌بعدی -->
    <div class="library-bg">
        <div class="floating-books">
            <div class="book-3d"></div>
            <div class="book-3d"></div>
            <div class="book-3d"></div>
            <div class="book-3d"></div>
            <div class="book-3d"></div>
            <div class="book-3d"></div>
            <div class="book-3d"></div>
            <div class="book-3d"></div>
        </div>
    </div>

    <!-- محتوای اصلی -->
    <div class="main-content" id="main-content">
        <!-- هدر پیشرفته -->
        <header class="advanced-header">
            <div style="display: flex; align-items: center; gap: 15px;">
                <span style="font-size: 2.5rem;">📚</span>
                <div>
                    <h1 style="font-size: 1.8rem; background: linear-gradient(135deg, #fff, var(--gold)); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">کتابخانه چشم‌انداز</h1>
                    <small style="color: var(--jade-soft);">Eye Landgoi Advanced Library</small>
                </div>
            </div>
            <div class="header-stats">
                <div class="stat-item">
                    <span>📖</span>
                    <span class="stat-number" id="dictionary-count">۲,۴۵۰+</span>
                    <span>واژه</span>
                </div>
                <div class="stat-item">
                    <span>🤖</span>
                    <span class="stat-number">AI</span>
                    <span>ربات فعال</span>
                </div>
            </div>
        </header>

        <!-- کتاب دو صفحه‌ای با افکت 3D -->
        <div class="book-3d-container">
            <!-- صفحه فارسی -->
            <div class="book-page-advanced" style="--rotate-y: 0deg;">
                <div class="page-decoration">📜</div>
                <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 25px;">
                    <span style="font-size: 2.5rem;">🇮🇷</span>
                    <div>
                        <h2 style="font-size: 2rem;">فارسی</h2>
                        <small style="color: var(--jade-soft);">Persian</small>
                    </div>
                </div>
                
                <div class="translation-box-advanced">
                    <div class="translation-label-advanced">
                        <span>✍️ متن خود را بنویسید</span>
                        <span id="fa-word-count">۰ واژه</span>
                    </div>
                    <textarea id="farsi-text" placeholder="اینجا بنویسید..."></textarea>
                </div>

                <div class="translation-box-advanced">
                    <div class="translation-label-advanced">
                        <span>🔍 ترجمه انگلیسی</span>
                        <span>🇬🇧</span>
                    </div>
                    <textarea id="english-translated" placeholder="ترجمه انگلیسی..." readonly></textarea>
                </div>
            </div>

            <!-- صفحه انگلیسی -->
            <div class="book-page-advanced" style="--rotate-y: 0deg;">
                <div class="page-decoration">📜</div>
                <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 25px;">
                    <span style="font-size: 2.5rem;">🇬🇧</span>
                    <div>
                        <h2 style="font-size: 2rem;">English</h2>
                        <small style="color: var(--jade-soft);">انگلیسی</small>
                    </div>
                </div>

                <div class="translation-box-advanced">
                    <div class="translation-label-advanced">
                        <span>✍️ Write your text</span>
                        <span id="en-word-count">۰ words</span>
                    </div>
                    <textarea id="english-text" placeholder="Write here..."></textarea>
                </div>

                <div class="translation-box-advanced">
                    <div class="translation-label-advanced">
                        <span>🔍 Persian Translation</span>
                        <span>🇮🇷</span>
                    </div>
                    <textarea id="farsi-translated" placeholder="ترجمه فارسی..." readonly></textarea>
                </div>
            </div>
        </div>

        <!-- پنل دکمه‌های پیشرفته -->
        <div class="button-panel">
            <button class="btn-advanced btn-primary-advanced" id="translate-fa-to-en">
                <span>🇮🇷 ➡️ 🇬🇧</span> ترجمه فارسی به انگلیسی
            </button>
            <button class="btn-advanced btn-primary-advanced" id="translate-en-to-fa">
                <span>🇬🇧 ➡️ 🇮🇷</span> Translate to Persian
            </button>
            <button class="btn-advanced" id="swap-texts">
                <span>🔄</span> جابجایی متن‌ها
            </button>
            <button class="btn-advanced btn-gold-advanced" id="clear-all">
                <span>🗑️</span> پاک کردن
            </button>
        </div>

        <!-- دیکشنری پیشرفته -->
        <div class="dictionary-advanced">
            <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 25px;">
                <span style="font-size: 2.5rem;">📖</span>
                <h2 style="font-size: 1.8rem; color: var(--gold);">دیکشنری هوشمند</h2>
            </div>
            
            <div class="search-advanced">
                <input type="text" class="search-input-advanced" id="dictionary-search" placeholder="جستجوی کلمه (فارسی یا انگلیسی)...">
                <button class="btn-advanced" id="search-btn" style="flex: 0 0 auto; padding: 18px 40px;">🔍 جستجو</button>
            </div>

            <div class="dictionary-grid-advanced" id="dictionary-grid">
                <!-- لغات توسط جاوااسکریپت پر می‌شوند -->
            </div>
        </div>

        <!-- فوتر -->
        <footer class="advanced-footer">
            <p>کتابخانه مترجم چشم انداز گویی | Eye Landgoi Translation Library</p>
            <p class="team-signature-advanced">✧ تیم سبز یشمی | Sabz Yashmi Team ✧</p>
            <p style="color: var(--jade-soft);">ربات هوشمند فعال | AI-Powered Translation</p>
        </footer>
    </div>

    <script>
        // ==================== دیکشنری کامل ====================
        const dictionary = [
            // افعال
            { fa: "رفتن", en: "go" },
            { fa: "آمدن", en: "come" },
            { fa: "دیدن", en: "see" },
            { fa: "گفتن", en: "say" },
            { fa: "خوردن", en: "eat" },
            { fa: "نوشیدن", en: "drink" },
            { fa: "خوابیدن", en: "sleep" },
            { fa: "دویدن", en: "run" },
            { fa: "نوشتن", en: "write" },
            { fa: "خواندن", en: "read" },
            { fa: "خریدن", en: "buy" },
            { fa: "فروختن", en: "sell" },
            { fa: "دادن", en: "give" },
            { fa: "گرفتن", en: "take" },
            { fa: "داشتن", en: "have" },
            { fa: "بودن", en: "be" },
            { fa: "توانستن", en: "can" },
            { fa: "خواستن", en: "want" },
            { fa: "دانستن", en: "know" },
            
            // اسامی
            { fa: "کتاب", en: "book" },
            { fa: "خانه", en: "house" },
            { fa: "ماشین", en: "car" },
            { fa: "دوست", en: "friend" },
            { fa: "خانواده", en: "family" },
            { fa: "مادر", en: "mother" },
            { fa: "پدر", en: "father" },
            { fa: "برادر", en: "brother" },
            { fa: "خواهر", en: "sister" },
            { fa: "پسر", en: "son" },
            { fa: "دختر", en: "daughter" },
            { fa: "مرد", en: "man" },
            { fa: "زن", en: "woman" },
            { fa: "کودک", en: "child" },
            { fa: "آب", en: "water" },
            { fa: "غذا", en: "food" },
            { fa: "چای", en: "tea" },
            { fa: "قهوه", en: "coffee" },
            { fa: "نان", en: "bread" },
            { fa: "برنج", en: "rice" },
            { fa: "میوه", en: "fruit" },
            { fa: "سیب", en: "apple" },
            { fa: "موز", en: "banana" },
            { fa: "پرتقال", en: "orange" },
            { fa: "انسان", en: "human" },
            { fa: "ربات", en: "robot" },
            { fa: "هوش", en: "intelligence" },
            { fa: "کامپیوتر", en: "computer" },
            
            // صفات
            { fa: "خوب", en: "good" },
            { fa: "بد", en: "bad" },
            { fa: "بزرگ", en: "big" },
            { fa: "کوچک", en: "small" },
            { fa: "گرم", en: "hot" },
            { fa: "سرد", en: "cold" },
            { fa: "جدید", en: "new" },
            { fa: "قدیمی", en: "old" },
            { fa: "زیبا", en: "beautiful" },
            { fa: "زشت", en: "ugly" },
            { fa: "تمیز", en: "clean" },
            { fa: "کثیف", en: "dirty" },
            { fa: "سریع", en: "fast" },
            { fa: "آرام", en: "slow" },
            { fa: "هوشمند", en: "smart" },
            { fa: "قوی", en: "strong" },
            { fa: "ضعیف", en: "weak" },
            
            // قیدها
            { fa: "امروز", en: "today" },
            { fa: "فردا", en: "tomorrow" },
            { fa: "دیروز", en: "yesterday" },
            { fa: "الان", en: "now" },
            { fa: "بعدا", en: "later" },
            { fa: "همیشه", en: "always" },
            { fa: "هرگز", en: "never" },
            { fa: "گاهی", en: "sometimes" },
            
            // عبارات رایج
            { fa: "سلام", en: "hello" },
            { fa: "خداحافظ", en: "goodbye" },
            { fa: "صبح بخیر", en: "good morning" },
            { fa: "شب بخیر", en: "good night" },
            { fa: "چطوری", en: "how are you" },
            { fa: "خوبم", en: "I'm fine" },
            { fa: "ممنون", en: "thanks" },
            { fa: "خواهش میکنم", en: "you're welcome" },
            { fa: "بله", en: "yes" },
            { fa: "خیر", en: "no" },
            { fa: "لطفا", en: "please" },
            { fa: "ببخشید", en: "excuse me" },
            { fa: "متاسفم", en: "sorry" },
            { fa: "اسم من", en: "my name is" },
            { fa: "اهل کجایی", en: "where are you from" },
            { fa: "چقدر", en: "how much" },
            { fa: "کجا", en: "where" },
            { fa: "کی", en: "when" },
            { fa: "چرا", en: "why" },
            { fa: "چطور", en: "how" },
            { fa: "من عاشق تو هستم", en: "I love you" },
            
            // تخصصی
            { fa: "چشم انداز", en: "landscape" },
            { fa: "سبز یشمی", en: "jade green" },
            { fa: "تیم", en: "team" },
            { fa: "مترجم", en: "translator" },
            { fa: "کتابخانه", en: "library" },
            { fa: "سایت", en: "website" },
            { fa: "چشم انداز گویی", en: "Eye Landgoi" },
            { fa: "ربات", en: "robot" },
            { fa: "هوش مصنوعی", en: "artificial intelligence" },
        ];

        // ==================== متغیرها ====================
        const loadingScreen = document.getElementById('loading-screen');
        const mainContent = document.getElementById('main-content');
        const robotRecognition = document.getElementById('robot-recognition');
        const robotMessage = document.getElementById('robot-message');
        
        const farsiText = document.getElementById('farsi-text');
        const englishText = document.getElementById('english-text');
        const farsiTranslated = document.getElementById('farsi-translated');
        const englishTranslated = document.getElementById('english-translated');
        const faWordCount = document.getElementById('fa-word-count');
        const enWordCount = document.getElementById('en-word-count');
        const dictionaryGrid = document.getElementById('dictionary-grid');
        const dictionarySearch = document.getElementById('dictionary-search');
        const searchBtn = document.getElementById('search-btn');

        // پیام‌های ربات
        const robotMessages = [
            "👋 سلام! من ربات چشم‌انداز گویی هستم",
            "🤖 می‌تونم بهت ترجمه یاد بدم",
            "📚 کتابخانه من پر از کلماته",
            "💚 تیم سبز یشمی عالیه",
            "🔍 دنبال چی می‌گردی؟",
            "🌐 فارسی به انگلیسی و برعکس",
            "⚡ هوش مصنوعی فعال",
            "🎯 کلیک کن ببین چکار می‌کنم"
        ];

        let messageIndex = 0;

        // ==================== صفحه لودینگ ====================
        setTimeout(() => {
            loadingScreen.classList.add('hidden');
            mainContent.classList.add('visible');
            
            // فعال‌سازی ربات بعد از لود
            startRobotMessages();
        }, 3000);

        // ==================== ربات ====================
        function startRobotMessages() {
            setInterval(() => {
                messageIndex = (messageIndex + 1) % robotMessages.length;
                robotMessage.textContent = robotMessages[messageIndex];
            }, 5000);
        }

        // حرکت ربات با ماوس
        document.addEventListener('mousemove', (e) => {
            const robot = document.getElementById('robot-recognition');
            const x = (window.innerWidth - e.pageX) / 50;
            const y = (window.innerHeight - e.pageY) / 50;
            robot.style.transform = `translate(${x}px, ${y}px)`;
        });

        // کلیک روی ربات
        robotRecognition.addEventListener('click', () => {
            robotMessage.style.background = 'rgba(100, 200, 100, 0.9)';
            robotMessage.textContent = '🤖 بله قربان! آماده‌ام';
            setTimeout(() => {
                robotMessage.style.background = 'rgba(10, 40, 20, 0.9)';
            }, 1000);
        });

        // ==================== ذرات معلق ====================
        function createParticles() {
            const container = document.getElementById('particles-container');
            for (let i = 0; i < 30; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 5 + 's';
                particle.style.background = i % 2 === 0 ? 'var(--gold)' : 'var(--jade-light)';
                container.appendChild(particle);
            }
        }
        createParticles();

        // ==================== توابع کمکی ====================
        function updateWordCount() {
            faWordCount.textContent = `${farsiText.value.trim() ? farsiText.value.trim().split(/\s+/).length : 0} واژه`;
            enWordCount.textContent = `${englishText.value.trim() ? englishText.value.trim().split(/\s+/).length : 0} words`;
        }

        // ترجمه با دیکشنری
        function translateWithDictionary(text, fromLang) {
            if (!text.trim()) return '';
            
            const words = text.split(/(\s+|[.،,!?؟])/g);
            const translatedWords = words.map(word => {
                if (word.trim() === '' || /[.،,!?؟]/.test(word)) return word;
                
                if (fromLang === 'fa') {
                    const found = dictionary.find(item => item.fa === word);
                    return found ? found.en : word;
                } else {
                    const found = dictionary.find(item => item.en.toLowerCase() === word.toLowerCase());
                    return found ? found.fa : word;
                }
            });
            
            return translatedWords.join('');
        }

        // ترجمه با API
        async function translateWithAPI(text, fromLang, toLang) {
            const url = `https://api.mymemory.translated.net/get?q=${encodeURIComponent(text)}&langpair=${fromLang}|${toLang}`;
            
            try {
                const response = await fetch(url);
                const data = await response.json();
                
                if (data.responseData && data.responseData.translatedText) {
                    return data.responseData.translatedText;
                }
                return null;
            } catch (error) {
                console.error('خطای API:', error);
                return null;
            }
        }

        // ترجمه اصلی
        async function performTranslation(fromLang) {
            let sourceText, targetField, from, to;
            
            if (fromLang === 'fa') {
                sourceText = farsiText.value;
                targetField = englishTranslated;
                from = 'fa';
                to = 'en';
            } else {
                sourceText = englishText.value;
                targetField = farsiTranslated;
                from = 'en';
                to = 'fa';
            }

            if (!sourceText.trim()) {
                alert('لطفاً متنی را وارد کنید');
                return;
            }

            // اول با دیکشنری
            const dictTranslation = translateWithDictionary(sourceText, fromLang);
            
            // بعد با API (برای کلمات جدید)
            const apiTranslation = await translateWithAPI(sourceText, from, to);
            
            if (apiTranslation) {
                targetField.value = apiTranslation;
                
                // پیام ربات
                robotMessage.textContent = '🤖 ترجمه با API انجام شد!';
                setTimeout(() => {
                    robotMessage.textContent = robotMessages[0];
                }, 2000);
            } else {
                targetField.value = dictTranslation;
                robotMessage.textContent = '📚 ترجمه با دیکشنری';
            }
        }

        // ==================== رویدادها ====================
        document.getElementById('translate-fa-to-en').addEventListener('click', () => performTranslation('fa'));
        document.getElementById('translate-en-to-fa').addEventListener('click', () => performTranslation('en'));

        document.getElementById('swap-texts').addEventListener('click', () => {
            const tempFa = farsiText.value;
            const tempEn = englishText.value;
            
            farsiText.value = tempEn;
            englishText.value = tempFa;
            
            farsiTranslated.value = '';
            englishTranslated.value = '';
            
            updateWordCount();
            robotMessage.textContent = '🔄 متن‌ها جابجا شدند';
        });

        document.getElementById('clear-all').addEventListener('click', () => {
            farsiText.value = '';
            englishText.value = '';
            farsiTranslated.value = '';
            englishTranslated.value = '';
            updateWordCount();
            robotMessage.textContent = '🧹 همه پاک شد';
        });

        farsiText.addEventListener('input', updateWordCount);
        englishText.addEventListener('input', updateWordCount);

        // ==================== دیکشنری ====================
        function renderDictionary(filter = '') {
            const filtered = dictionary.filter(item => 
                item.fa.includes(filter) || 
                item.en.toLowerCase().includes(filter.toLowerCase())
            );
            
            dictionaryGrid.innerHTML = filtered.map(item => `
                <div class="word-card-advanced" onclick="useWord('${item.fa}', '${item.en}')">
                    <span class="word-fa-advanced">${item.fa}</span>
                    <span style="color: var(--gold);">➡️</span>
                    <span class="word-en-advanced">${item.en}</span>
                </div>
            `).join('');
            
            document.getElementById('dictionary-count').textContent = `${dictionary.length}+`;
        }

        window.useWord = function(fa, en) {
            if (document.activeElement === farsiText || document.activeElement === farsiTranslated) {
                farsiText.value += (farsiText.value ? ' ' : '') + fa;
            } else if (document.activeElement === englishText || document.activeElement === englishTranslated) {
                englishText.value += (englishText.value ? ' ' : '') + en;
            } else {
                farsiText.value += (farsiText.value ? ' ' : '') + fa;
                englishText.value += (englishText.value ? ' ' : '') + en;
            }
            updateWordCount();
            robotMessage.textContent = `➕ کلمه "${fa}" اضافه شد`;
        };

        searchBtn.addEventListener('click', () => renderDictionary(dictionarySearch.value));
        dictionarySearch.addEventListener('keyup', (e) => {
            if (e.key === 'Enter') renderDictionary(dictionarySearch.value);
        });

        // ==================== میانبرها ====================
        document.addEventListener('keydown', (e) => {
            if (e.ctrlKey && e.key === 'Enter') {
                e.preventDefault();
                if (document.activeElement === farsiText) performTranslation('fa');
                else if (document.activeElement === englishText) performTranslation('en');
            }
            
            if (e.ctrlKey && e.shiftKey && e.key === 'S') {
                e.preventDefault();
                document.getElementById('swap-texts').click();
            }
        });

        // ==================== مقداردهی اولیه ====================
        renderDictionary();
        updateWordCount();

        // متن نمونه
        farsiText.value = "سلام ربات! امروز می‌خواهم درباره چشم انداز گویی یاد بگیرم.";
        englishText.value = "Hello robot! Today I want to learn about Eye Landgoi.";
        updateWordCount();
    </script>
</body>
</html>